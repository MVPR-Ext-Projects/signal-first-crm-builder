export * from "./types"
// TODO(template): the opinionated methodology template was removed during the
// gtm-os-template fork. The wizard pipeline (apps/web/app/api/agent/analyze
// + apps/web/app/api/chat) depended on its exports. Re-introduce a generic
// methodology template here when the wizard is rebuilt.
